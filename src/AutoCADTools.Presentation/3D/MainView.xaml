<Window x:Class="AutoCADTools.Presentation._3D.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:AutoCADTools.Presentation._3D"
        xmlns:canvas="clr-namespace:AutoCADTools.Presentation.Canvas"
        mc:Ignorable="d"
        Title="AutoCAD Tools" Height="700" Width="1100"
        MinHeight="500" MinWidth="800"
        Background="#FF1E1E1E"
        WindowStartupLocation="CenterScreen">
  <TabControl Background="#FF1E1E1E" BorderThickness="0">
    <!-- 2D Canvas tab -->
    <TabItem Header="2D Canvas">
      <canvas:CanvasView DataContext="{Binding CanvasViewModel}"/>
    </TabItem>

    <!-- 3D View tab -->
    <TabItem Header="3D View">
      <local:Viewport3DControl DataContext="{Binding ViewportWindowViewModel}"/>
    </TabItem>
  </TabControl>
</Window>
